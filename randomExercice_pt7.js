// "Last project ;)"

// return fraction in string 

function fraction(numerirator, denomirator){
    const res = numerirator/denomirator;
    return res.toString();
}

console.log( typeof fraction(1,2))